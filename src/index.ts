/**
 * GitHub App Token Service
 *
 * Generates scoped GitHub App installation tokens for devpods
 * without exposing the GitHub App private key.
 */

const PORT = process.env.PORT ?? 3000;

console.log(`GitHub Token Service starting on port ${PORT}...`);
console.log("Service not yet implemented - see plan.md for architecture");
